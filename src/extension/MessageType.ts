enum MessageType {
    MEMBER_JOIN,
    MEMBER_LEFT,
    ITEM_FOCUSED,
    VOTING_STATUS,
    VOTING_RESULT,
    MEMBER_VOTED,
    ITEM_ESTIMATED,
    PLANNING_COMPLETED
};

export default MessageType;
